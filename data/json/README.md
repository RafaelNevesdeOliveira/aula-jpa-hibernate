# Seed JSON

Fonte dos dados da aula. Cada execucao do `App` aplica estes arquivos no H2.

| Arquivo | Tabela |
| --- | --- |
| `contas.json` | `conta` |
| `pessoas.json` | `pessoa` |

Edite os arrays e rode o `App` de novo. Os exercicios consomem o que estiver aqui.
